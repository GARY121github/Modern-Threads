<nav class="nav-bar">
    <button class="fs-2">
        <a href="/" style="text-decoration : none;">Modern Threads</a>
    </button>

    <% if(user){ %>
        <ul>
            <li><a href="/">Products</a></li>
            <% if(user.role==='seller' ){ %>
                <li><a href="/create">Create</a></li>
                <% } %>
        </ul>
        <form>
            <i class="fa-solid fa-magnifying-glass"></i>
            <input type="text" placeholder="Search for products...">
        </form>
        <% } %>
            <div>
                <% if(user){ %>
                    <a href="/cart"><i class="fa-solid fa-cart-shopping"><sup><span id="sup-item">
                                    <%=user.cart.length%>
                                </span></sup></i></a>
                    <a href="/auth/logout">Logout <i class="fa-solid fa-arrow-right-from-bracket"></i></a>

                        <% }else{ %>
                            <button class="fs-5">
                                <a href="/auth/login" style="text-decoration : none;">Login</a>
                            </button>
                            <button class="fs-5">
                                <a href="/auth/register" style="text-decoration : none;">Signup</a>
                            </button>
                            <% } %>

            </div>
</nav>